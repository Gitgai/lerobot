# State Of The Project — end of 2026-08-06

Read this first. Yesterday's entry point (`STATE_20260805.md`) records how the
simulation stack was built and verified; this one records what the two days
since produced and exactly what happens next. The active plan remains
`PLAN_real_arm_via_gr00t_20260806.md`.

---

## 1. What is now TRUE and VERIFIED

```text
A POLICY DOES THE TASK (in sim)
  12e21/gr00t_n1d6_leisaac_pick_orange - GR00T N1.6, fine-tuned inside LeIsaac,
  driven by LeIsaac's NATIVE client (none of our adapter code in the path).
  Honest rate across ALL 12 full-length canonical runs: 34/36 oranges (94%),
  10/12 full completions, 1.1 drops/run. It fumbles and recovers; the user
  spotted that from the GUI before the statistics did.
  -> gr00t_n16_sim_trained_SUCCESS_20260805.md

THE MEASUREMENT IS TRUSTWORTHY
  positive control (scripted state machine through the same scoring path):
  grasp, place and lift all detected. Every failure recorded is a real failure.
  Grasp claims REQUIRE object displacement: real ~0.17-0.20 m, fake ~0.003 m.

WHAT THE POLICY ACTUALLY ATTENDS TO (robustness campaign, 17 runs)
  appearance (colors, lighting, even the robot recolored)   FREE
  geometry (objects/goal moved, layout scattered)           ~one orange
  camera mount position                                     2 cm free, 5 cm ok
  ORANGE-COLORED DECOYS                                     FATAL (1/3)
  -> n16_robustness_campaign_20260806.md

THE SIM-TO-REAL PREFLIGHT (standing protocol, first run COMPLETE)
  Stage A - the real-arm client is VERIFIED field-by-field against the
    validated sim client, live handshake included. lerobot 0.4.4 (matches the
    calibration's own era), 2-line import patch, torch untouched.
  Stage B - failure signatures, one defect per run:
    BGR channel swap    INVISIBLE (3/3!)  -> no behavioral signature exists;
                        source-level client verification is the ONLY guard
    gamma/white-balance THE KILLER (0/3)  -> worst run of the whole preflight
    noise, staleness, wrist jitter        free
    blur, jpeg, 5-degree camera pitch     ~one orange (baseline variance)
  Stage C - single-orange scene PASSES (1/1).
  => the policy keys on SATURATED-BLOB STRUCTURE, not hue. Hue permutation is
     free; contrast/saturation wash is fatal; a second orange-blob candidate
     is fatal.
  -> sim_to_real_preflight_protocol_20260806.md
```

## 2. THE NEXT ACTION — hardware, user's half

Everything software-side is verified and waiting. The physical checklist:

```text
1. Plug in the ARM            (expect /dev/ttyACM0; calibration file already
                               in place: so_follower/my_so101_follower.json)
2. Plug in FRONT + WRIST cameras (only two - GR00T does not use `top`).
   Wrist previously came via the RPi bridge; re-check on this machine.
3. MOUNT the front camera on the ROBOT BASE, pos ~(0.0, -0.5, 0.6) rel. base.
   2 cm of slack is proven; careful, not obsessive. Avoid large ANGLE error
   (5 degrees cost an orange).
4. *** LOCK WHITE BALANCE + EXPOSURE on both cameras ***
   (v4l2-ctl -d /dev/videoN -c white_balance_automatic=0 -c auto_exposure=1,
   or cv2 CAP_PROP_AUTO_WB=0). THE one observation defect that broke the
   policy (0/3) is exactly what a webcam's auto-WB does continuously.
5. *** CLEAN TABLE *** - nothing orange-ish anywhere near the workspace.
   1 or 3 oranges + the plate; both scene sizes are proven.
```

Then the as-is test (me): N1.6 server on :5556 + `eval_so100.py` with the
so101_follower config, instruction "Grab orange and place into plate",
recorded, scored by the finger-stall test. Expectation set by evidence:
purposeful reach = success; completion = surprise.

## 3. Parked work (deliberately, not forgotten)

```text
FINE-TUNE TRACK (plan steps 2-4, activates if/when the as-is test fails)
  - memory ceiling: 26.0 GB floor vs 31.35 GB usable, dies in Adam. FIX READY
    TO TRY: 8-bit Adam (adamw_bnb_8bit + bitsandbytes) -> floor ~16.4 GB.
  - our 89 real episodes need v3.0 -> GR00T v2 conversion + `top` camera drop.
  - LightwheelAI dataset is prepped (GR00T-flavored, AV1 transcoded to H.264).
SIM ODDS AND ENDS (non-gating)
  - --add-plate flag is BROKEN (deepcopy of a parse_usd cfg is not a clone)
  - decoy failure not yet characterized (grabs decoys, or freezes?)
  - Pi05 and N1.7 3,000-step re-runs died early (server OOM during the leak);
    their "0 grasps" verdicts stand on shorter horizons only.
```

## 4. The unchanged, load-bearing truth

```text
NOTHING HAS BEEN TESTED ON THE REAL ARM - now three days running.
Every result above is simulation. The entire preflight exists so that when the
arm finally moves, what we see is the POLICY meeting reality - not a channel
swap, a unit bug, or a drifting white balance.
```
