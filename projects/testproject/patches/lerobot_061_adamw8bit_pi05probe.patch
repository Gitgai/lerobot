# lerobot 0.6.1 -> 8-bit AdamW for the pi05 full-fine-tune memory probe
# Generated 2026-08-11. Apply to: sim/pi05-fullft-probe/.venv312/.../lerobot
# Rationale + the silent-override trap it defends: docs/pi05_full_finetune_on_5090_plan_20260811.md §3
# Revert: cp the .orig files back (kept alongside in site-packages)

--- b/lerobot/optim/optimizers.py	2026-08-11 17:10:06.994721385 -0400
+++ b/lerobot/optim/optimizers.py	2026-08-11 17:10:07.005677275 -0400
@@ -111,6 +111,30 @@
         return torch.optim.AdamW(params, **kwargs)


+# --- PROBE PATCH: pi05 full fine-tune on a 32 GB 5090 ---------------------
+# docs/pi05_full_finetune_on_5090_plan_20260811.md
+# FP32 AdamW holds m+v at 8 bytes/param = 30.8 GiB for pi05's 4.14B, which alone
+# exceeds the card. 8-bit blockwise states cut that to ~7.7 GiB.
+@OptimizerConfig.register_subclass("adamw_8bit")
+@dataclass
+class AdamW8bitConfig(OptimizerConfig):
+    lr: float = 1e-3
+    betas: tuple[float, float] = (0.9, 0.999)
+    eps: float = 1e-8
+    weight_decay: float = 1e-2
+    grad_clip_norm: float = 10.0
+
+    def build(self, params: OptimizerParams) -> torch.optim.Optimizer:
+        import bitsandbytes as bnb
+
+        kwargs = asdict(self)
+        kwargs.pop("grad_clip_norm")
+        return bnb.optim.AdamW8bit(params, **kwargs)
+
+
+# --- END PROBE PATCH ------------------------------------------------------
+
+
 @OptimizerConfig.register_subclass("sgd")
 @dataclass
 class SGDConfig(OptimizerConfig):
--- b/lerobot/policies/pi05/configuration_pi05.py	2026-08-11 17:10:06.995375571 -0400
+++ b/lerobot/policies/pi05/configuration_pi05.py	2026-08-11 17:10:07.005769708 -0400
@@ -146,7 +146,12 @@
             self.output_features[ACTION] = action_feature

     def get_optimizer_preset(self) -> AdamWConfig:
-        return AdamWConfig(
+        # PROBE PATCH: validate() overwrites any --optimizer.type from the CLI
+        # with this preset whenever use_policy_training_preset is True (default).
+        # Patching here is the only route that cannot be silently discarded.
+        from lerobot.optim.optimizers import AdamW8bitConfig
+
+        return AdamW8bitConfig(
             lr=self.optimizer_lr,
             betas=self.optimizer_betas,
             eps=self.optimizer_eps,
