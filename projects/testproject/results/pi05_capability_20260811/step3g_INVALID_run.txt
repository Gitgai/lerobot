# STEP 3g — INVALID RUN. 7 h wasted. ⚠ THE RECORDED CAUSE WAS WRONG — see below.

config as launched:
  from lerobot/pi05_libero_base · bs8 x accum4 · 24,000 batches
  --rename_map '{"observation.images.wrist_image": "observation.images.image2"}'

########################################################################
# ⛔ RETRACTION — 2026-08-14. The mechanism below is REFUTED by direct
# measurement. Kept verbatim because a wrong explanation that shaped a
# safety gate has to stay visible, not be quietly overwritten.
########################################################################

WHAT THIS FILE ORIGINALLY CLAIMED:
  "lerobot builds input_features FROM THE DATASET -> image, wrist_image
   --rename_map rewrites the BATCH                -> image, image2
   => wrist_image MISSING from all 24,000 batches"

WHY THAT IS WRONG:
  input_features come from the DATASET only for a FRESH policy. For a
  PRETRAINED one (--policy.path=...) they come from THE CHECKPOINT. lerobot
  states this itself in configs/train.py:249:

    "`rename_map` requires a pretrained policy checkpoint. Fresh
     initialization derives feature names from the current dataset, so no
     rename is applied."

  MEASURED, 2026-08-14 (preflight_features.py, case B):
    lerobot/pi05_libero_base expects  image, image2      <- from the checkpoint
    dataset provides                  image, wrist_image
    after the rename the batch carries image, image2     <- MATCHES

  ⇒ The wrist camera was NOT missing. 3g's training input was camera-correct.
  ⇒ The "unplanned ablation" this file claimed to salvage (pi05 minus the wrist
    camera scores 1-3%) IS NOT SUPPORTED. Do not cite it.

WHAT REMAINS TRUE AND MEASURED:
  loss 0.052 vs a healthy run's 0.062
  eval  3.0% (with baked rename) / 1.0% (baked rename cleared) vs 64.5%

WHAT IS NOW UNEXPLAINED:
  Why the run scored 1-3%. The leading candidate is an EVAL-side name mismatch,
  not a training-side one: the resulting checkpoint expects image/image2, while
  the LIBERO env supplies image/wrist_image, and pi05 pads whatever is missing.
  That is a HYPOTHESIS. It is not tested, and it cannot be tested from here —
  the 3g checkpoints were deleted (~180 GB) before this was understood.

# THE RUN STILL MEASURED NOTHING about the starting-checkpoint hypothesis.
# The 7 hours are still lost. What changed is WHY, and therefore what prevents
# a repeat: not "never rename" but "verify against the CHECKPOINT's feature
# names, and know that supplying a rename_map turns lerobot's own visual
# validation OFF" (policies/factory.py:382).
#
# Prevention: preflight_features.py. preflight_batch_check.py is SUPERSEDED —
# it encoded the fresh-init rule universally and gave a FALSE PASS on
# 2026-08-14 for a pretrained config lerobot then refused to start.
