# STEP 3g — INVALID RUN. 7 h wasted; the wrist camera was blanked throughout.

config as launched:
  from lerobot/pi05_libero_base · bs8 x accum4 · 24,000 batches
  --rename_map '{"observation.images.wrist_image": "observation.images.image2"}'

what actually happened:
  lerobot builds input_features FROM THE DATASET -> image, wrist_image
  --rename_map rewrites the BATCH                -> image, image2
  => wrist_image MISSING from all 24,000 batches
  => pi05 PADS missing cameras with -1 and MASKS them. No error. No log line.

the signature, and why it fooled me:
  loss 0.052 vs a healthy run's 0.062 - ~18% BETTER at every checkpoint,
  because one camera is a simpler function to fit.
  eval: 3.0% (with baked rename) / 1.0% (baked rename cleared) vs 64.5%

# THE RUN MEASURED NOTHING about the starting-checkpoint hypothesis.
# Salvageable: an unplanned ablation - pi05 on LIBERO Spatial with the
# wrist camera removed scores 1-3% vs 64.5% with both cameras.
# Prevention: preflight_batch_check.py (this directory), runs in seconds.
